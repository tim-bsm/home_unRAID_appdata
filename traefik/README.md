# Traefik

This configuration provides a quick reverse proxy to secure the connections with `TLS` for `HTTP and TCP`. It is used to secure the webpages of the different docker containers and the `mqtt` connection.