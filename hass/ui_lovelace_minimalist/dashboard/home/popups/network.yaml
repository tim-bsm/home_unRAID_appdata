---

# Show network specific settings and infos in pop-up
view_layout:
  grid-area: "popup"
type: "vertical-stack"
cards:
  - type: "custom:bubble-card"
    card_type: "pop-up"
    hash: "#network"
    name: "Network"
    icon: "mdi:lan"
    width_desktop: "80%"
    shadow_opacity: 70
    # Make bubble card look similar to minimalist
    styles: |
      .bubble-button-card-container {
        background: var(--card-background-color) !important;
      }
      .bubble-icon {
        opacity: 1 !important;
      }
      .bubble-close-button{
        background: var(--card-background-color) !important;
      }
      
  
  ###
  # WiFi QR code tab
  ###

  # Add separator
  - type: "custom:bubble-card"
    card_type: "separator"
    name: "Scan to connect to the WiFi"
    icon: "mdi:wifi"
  
  # Use custom:mod-card to remove space created by nesting grids
  # https://github.com/thomasloven/lovelace-layout-card/issues/137#issuecomment-1789629405
  - type: "custom:mod-card"
    card_mod:
      style:
        layout-card$:
          grid-layout$: |
            :host {
              padding: 0rem !important;
            }
            #root {
              margin: 0rem !important;
            }
            #root > * {
              margin: 0.1rem !important;
            }
    card:
      type: "custom:layout-card"
      layout_type: "grid"
      layout:
        # Define the amount of columns
        grid-template-columns: "repeat(3, 1fr)"
        grid-template-rows: "min-content"
        grid-gap: "calc(0.7em + 5px)" # 5px to compensate removed space for nesting grids
        # Use grid-template-areas to define the layout
        # Place the QR code in the center
        grid-template-areas: |
          ".    qr_wifi   ."
        mediaquery:
          # Mobile
          "(max-width: 800px)":
            grid-template-columns: "100%"
            grid-template-rows: "min-content"
            grid-template-areas: |
              "qr_wifi"
      # Cards for each climate entity
      cards:
        - !include "network/qr_wifi.yaml"
    