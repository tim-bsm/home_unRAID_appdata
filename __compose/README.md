# `docker compose` files

This directory contains the `compose.yaml` files to start a docker container using the `docker compose` service. Moreover, further files regarding the docker configuration of a container (such aus `.env`/`*.labels`) are located here as well.